services:
  app:
    image: python3.12:dev
    container_name: ${CONTAINER_NAME}
    ports:
      - "${HOST_PORT}:8000"
    volumes:
      - ..:/workspace
    working_dir: /workspace
    command: sleep infinity
